pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            bat 'aws cloudformation create-stack --stack-name ens3bucket --template-body file://s3encrypt.yaml --region ap-south-1'
              }
             }
            }
            }
