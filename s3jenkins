pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            bat 'aws cloudformation Create-Stack --stack-name s3bucket --template-body file://s3encrypt.yaml --region us-east-1'
              }
             }
            }
            }
